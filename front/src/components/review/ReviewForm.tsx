import { IReviewForm } from "@type/review";

export default function ReviewForm({ type, ...data }: IReviewForm) {
  // prop으로 받은 데이터를 input들의 value로 넣어주시고 생성/수정에 대한 분기처리는 type으로 처리해주세요.
  return <div></div>;
}
